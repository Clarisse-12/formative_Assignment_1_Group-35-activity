import 'package:flutter/material.dart';

class ALUColors {
  ALUColors._();

  // Original colors
  static const Color primary = Color(0xFF003366); // ALU blue
  static const Color accent = Color(0xFFFDB827); // Gold / accent
  static const Color background = Color(0xFFFFFFFF);
  static const Color text = Color(0xFF000000);

  // Extended color palette for dashboard and other screens
  static const Color primaryDark = Color(0xFF0A1A3A);
  static const Color backgroundDark = Color(0xFF0A1A3A);
  static const Color cardBackground = Color(0xFF1A2A4A);
  static const Color cardLightBackground = Color(0xFF1A2A4A);

  // Text colors
  static const Color textWhite = Color(0xFFFFFFFF);
  static const Color textGray = Color(0xFF9CA3AF);

  // Accent colors
  static const Color accentYellow = Color(0xFFFDB827);
  static const Color infoBlue = Color(0xFF3B82F6);
  static const Color successGreen = Color(0xFF10B981);
  static const Color warningRed = Color(0xFFEF4444);
}
